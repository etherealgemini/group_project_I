import com.autogen.controller.MainController;

public class AutoTestGeneratorClient {
    public static void main(String[] args) {
        MainController controller = new MainController();
        controller.launch();
    }
}
